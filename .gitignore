# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Models (large binaries) - keep dir via .gitkeep
models/*.pkl
models/*.joblib

# Data / results (generated) - keep dirs via .gitkeep
data/*.csv
data/*.jsonl
results/*.csv
results/*.jsonl
# Intermediate per-node baseline harvest (superseded by results/real_normal_baseline_45m.csv)
data/real/
data/real2/
# Triage pipeline generated artifacts (regenerate via the pipeline; generator IS tracked)
data/cases/
data/labels/
data/features/*.jsonl
models/metrics.json
reports/*.json

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
