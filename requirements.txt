# Core ML / data
scikit-learn>=1.3,<2.0
numpy>=1.24
pandas>=2.0
joblib>=1.3

# API server
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Config
PyYAML>=6.0

# eBPF data collection (collector node only).
# bcc is installed via the system package manager (apt: bpfcc-tools python3-bpfcc),
# NOT via pip. Listed here for documentation only. See README.
# bcc
